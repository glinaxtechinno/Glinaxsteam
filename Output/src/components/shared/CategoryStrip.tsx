/**
 * components/shared/CategoryStrip.tsx
 * Category strip placeholder. Categories are rendered inline in app/page.tsx
 * to keep the homepage server-rendered. This file is a re-export stub
 * in case we need to extract the strip into a standalone component later.
 */

export { };
